//
//  TabbingChallenge.swift
//  SwiftUIHudson
//
//  Created by Lena Mattea Stöxen on 13.06.24.
///  Your job is to build a tiny part of the App Store: tabs across the bottom, plus Today-like zooming when an item is tapped. Don't go overboard – stay focused on the new APIs! – but this is a fun one to experiment with, particular to get the zoom effect right.

import SwiftUI

struct TabbingChallenge: View {
    var body: some View {
        Text(/*@START_MENU_TOKEN@*/"Hello, World!"/*@END_MENU_TOKEN@*/)
    }
}

#Preview {
    TabbingChallenge()
}
